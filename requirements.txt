# requirements.txt — drop-in replacement

# --- App runtime ---
streamlit>=1.36,<1.41           # avoids the new width API breakage while keeping modern features
pydantic>=2.6,<3
python-dotenv>=1.0,<2

# --- OpenAI stack (match with httpx 0.27.x) ---
openai>=1.30,<1.42              # stable v1 client APIs
httpx==0.27.2                   # IMPORTANT: 0.28+ removes 'proxies' kwarg used by openai
requests>=2.31,<3

# --- Data / ML ---
numpy>=1.26.4,<2
pandas>=2.2.2,<2.3
scikit-learn>=1.4.2,<1.5

# --- Parsing / scraping ---
beautifulsoup4>=4.12,<5
lxml>=4.9,<5

# --- Viz ---
plotly>=5.20,<6

# --- Optional niceties (safe to remove if you want lean) ---
tqdm>=4.66,<5
